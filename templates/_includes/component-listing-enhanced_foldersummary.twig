{% import '_macros.twig' as macros %}

<ul class="items listing list-unstyled {{ feed.layout }}">
{# {% if feed.items is defined and feed.items|length > 0 %}
{% for item in feed.items %}
{{item['@id']}}<br>
	{% set itemDetailParams = {
	    url: item['@id'],
	    type: 'json',
	    cache: 0,
	} %}
	{% set itemDetails = craft.feedme.feed(itemDetailParams) %}
	{{ dump(itemDetails)}}
{% endfor %}
{% endif %} #}
{% include "_includes/listing-items" with {items:feed.items,layout:feed.layout} %}


</ul>